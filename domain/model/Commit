function Commit(hash, author, author_email, date, message) {
    this.hash = hash
    this.author = author
    this.author_email = author_email
    this.date = date
    this.message = message
}

module.exports = Commit