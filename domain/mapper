const Commit = require('./model/Commit')

function mapToCommit(json) {
    return new Commit(
        json.hash,
        json.author,
        json.author_email,
        json.date,
        json.message
    )
}

module.exports.mapToCommit = mapToCommit